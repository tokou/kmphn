import SwiftUI
import Hackernews

struct CommentLoaderView: View {

    var body: some View {
        Text("...")
            .padding(16)
    }
}

struct CommentLoaderView_Previews: PreviewProvider {
    static var previews: some View {
        CommentLoaderView()
            .previewLayout(.sizeThatFits)
    }
}
